import type { ZoneModule } from '../zone';

/** Lobby / reception and the plaza furniture. (Placeholder: the shell already has the facade.) */
const zone: ZoneModule = {
  id: 'entrance',
  build() {
    return { slots: {} };
  },
};
export default zone;
